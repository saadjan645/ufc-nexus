import {mountNavbar} from '../components/navbar.js';import {mountFooter} from '../components/footer.js';import {mountLoader} from '../components/loader.js';import {initAnimations} from '../components/animations.js';import {api,demoFighters} from '../components/api.js';import {toast} from '../components/toast.js';
mountLoader();await mountNavbar('rankings');mountFooter();initAnimations();let rows=demoFighters.map(f=>({fighter:f,weightClass:f.division,score:f.wins*10+f.ko*3,wins:f.wins,ko:f.ko,submissions:f.submissions,accuracy:f.accuracy}));try{const d=await api('/leaderboard');rows=d.leaderboard?.length?d.leaderboard:rows}catch{toast('Backend offline: using demo rankings','info')}weightClass.innerHTML=['All',...new Set(rows.map(r=>r.weightClass))].map(c=>`<option>${c}</option>`).join('');weightClass.onchange=render;render();setInterval(()=>{rows=rows.map(r=>({...r,score:r.score+Math.floor(Math.random()*3)}));render()},9000);let c1,c2;function render(){const data=rows.filter(r=>weightClass.value==='All'||r.weightClass===weightClass.value).sort((a,b)=>b.score-a.score);rankRows.innerHTML=data.map((r,i)=>`<tr><td><span class="badge ${i<3?'gold':''}">#${i+1}</span></td><td>${r.fighter?.name||'Unknown'}</td><td>${r.weightClass}</td><td>${r.score}</td><td>${r.wins}</td><td>${r.ko}</td><td>${r.submissions}</td><td>${r.accuracy}%</td></tr>`).join('');draw(data.slice(0,7))}function draw(data){if(!window.Chart)return;const names=data.map(r=>r.fighter?.name?.split(' ').pop()||'Fighter');c1?.destroy();c2?.destroy();c1=new Chart(scoreChart,{type:'bar',data:{labels:names,datasets:[{label:'Score',data:data.map(r=>r.score)}]},options:{plugins:{legend:{labels:{color:'#fff'}}},scales:{x:{ticks:{color:'#aaa'}},y:{ticks:{color:'#aaa'}}}}});c2=new Chart(finishChart,{type:'line',data:{labels:names,datasets:[{label:'Finishes',data:data.map(r=>(r.ko||0)+(r.submissions||0)),tension:.35}]},options:{plugins:{legend:{labels:{color:'#fff'}}},scales:{x:{ticks:{color:'#aaa'}},y:{ticks:{color:'#aaa'}}}}})}
