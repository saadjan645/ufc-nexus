export function initAnimations(){customCursor();if(!window.gsap)return;gsap.from('.nexus-nav',{y:-80,opacity:0,duration:.9,ease:'power3.out'});gsap.from('[data-hero-line]',{y:80,opacity:0,stagger:.12,duration:1.1,ease:'power4.out',delay:.2});gsap.from('[data-reveal]',{y:70,opacity:0,stagger:.08,duration:.85,ease:'power3.out',delay:.35});document.addEventListener('mousemove',e=>{const x=(e.clientX/innerWidth-.5)*18,y=(e.clientY/innerHeight-.5)*18;document.querySelectorAll('[data-parallax]').forEach((el,i)=>gsap.to(el,{x:x*(i+1)*.15,y:y*(i+1)*.15,duration:.8,overwrite:true}))})}
export function beltUnlock(label='Champion Mode Unlocked'){const b=document.createElement('div');b.className='belt-unlock';b.innerHTML=`<span>🏆</span><strong>${label}</strong><small>Achievement unlocked</small>`;document.body.appendChild(b);setTimeout(()=>b.classList.add('show'),30);setTimeout(()=>b.remove(),4200)}
function customCursor(){if(matchMedia('(pointer:coarse)').matches)return;const d=document.createElement('div'),r=document.createElement('div');d.className='cursor-dot';r.className='cursor-ring';document.body.append(d,r);document.addEventListener('mousemove',e=>{d.style.transform=`translate(${e.clientX}px,${e.clientY}px)`;r.style.transform=`translate(${e.clientX}px,${e.clientY}px)`});document.addEventListener('mouseover',e=>r.classList.toggle('cursor-active',!!e.target.closest('a,button,input,select,textarea,.fighter-card')))}
